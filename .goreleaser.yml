project_name: 'galax-app'

before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod download
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
archives:
  - format: binary
checksum:
  name_template: 'checksums.txt'
changelog:
  sort: asc